{% extends 'loc/loc_base.html' %}


{% block content %}

	<table border=1>
		<tr>
		<th> 番地 </th>
		<th> コード </th>
		<th> 数量 </th>
		</tr>

	<div class="loc">
	{% for loc in locs %}
	<tr>
        <td class="banch">>
        <a href="{% url 'loc_detail' pk=loc.pk %}">{{ loc.banch }}</a> 
        </td>
        
	<td class="code">
	<h3>{{ loc.code }}</h3>
	</td>
	<td class="qty">
        {{ loc.qty }}
	</td>
	</tr>
	{% endfor %}
	</div>

	</table>


{% endblock %}

